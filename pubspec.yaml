name: vehicle_tracker
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  # Pure Dart Database (No SQLite C++ errors!)
  sembast: ^3.7.1
  path_provider: ^2.1.2

  # State Management
  flutter_riverpod: ^2.5.1

  # Utilities
  intl: ^0.19.0
  csv: ^6.0.0
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true